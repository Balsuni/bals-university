import Layout from "@/components/layout";
import PageHeader from "@/components/pageHeader";

const Home = () => {
  return (
    <Layout>
      <PageHeader />
    </Layout>
  );
};

export default Home;
