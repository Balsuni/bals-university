import React from 'react'

const FooterWrapper = ({children}) => {
  return (
    <footer className="bg-darkblue-500 relative bottom-0 w-screen h-auto py-10">{children}</footer>
  )
}

export default FooterWrapper